#/bin/bash


# Create default folder for projects
mkdir ~/workspace
cd ~/workspace

# Create default folder for configs
mkdir ~/config

# Create default folder for local changes
mkdir ~/config/local
touch ~/config/local/local.sh

# Create local temp
mkdir ~/config/temp
